apiVersion: v1
kind: Secret
metadata:
  name: bridge-secret
  namespace: pinkline
type: Opaque
# Replace all REPLACE values before applying
stringData:
  # artemis.username=${ARTEMIS_USER}
  # artemis.password=${ARTEMIS_PASS}
  ARTEMIS_USER: "REPLACE"
  ARTEMIS_PASS: "REPLACE"

  ARTEMIS_USER: "pasbridge"
  ARTEMIS_PASS: "testpass123"


  # spring.rabbitmq.username=${RABBITMQ_USER}
  # spring.rabbitmq.password=${RABBITMQ_PASS}
  RABBITMQ_USER: "admin"
  RABBITMQ_PASS: "admin"

  # bridge.mqtt.username=${MQTT_USER}
  # bridge.mqtt.password=${MQTT_PASS}
  MQTT_USER: "admin"
  MQTT_PASS: "admin"

  # EncryptProcessor reads SCADA_AES_KEY directly via System.getenv()
  # Must be same key as scada-api-secret SCADA_AES_KEY
  SCADA_AES_KEY: "k7Qh2NfT8vR0mC9aXy4pLwZbE3sG6uJtH1iKd5oArMw="

  # TLS — DevOps will add when ready:
  # TLS_TRUSTSTORE_PASS: "REPLACE"
