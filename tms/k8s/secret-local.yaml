apiVersion: v1
kind: Secret
metadata:
  name: bridge-secret
  namespace: pinkline
type: Opaque
# ── LOCAL test values — same as docker-compose.yml ────────────────────────
# DO NOT commit real production values here.
stringData:
  ARTEMIS_USER: "pasbridge"
  ARTEMIS_PASS: "testpass123"

  RABBITMQ_USER: "thiru"
  RABBITMQ_PASS: "password"

  MQTT_USER: "thiru"
  MQTT_PASS: "password"

  # Local test key — 32 zero bytes base64. NOT for production.
  SCADA_AES_KEY: "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="
