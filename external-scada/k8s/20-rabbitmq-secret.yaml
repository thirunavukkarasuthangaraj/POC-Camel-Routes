# ═══════════════════════════════════════════════════════════════════
# RabbitMQ credentials
# DevOps: rotate these to real values before applying to prod.
# Username and password must match what bridge-secret uses in tms/k8s.
# ═══════════════════════════════════════════════════════════════════
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-secret
  namespace: scada
type: Opaque
stringData:
  RABBITMQ_DEFAULT_USER: "thiru"
  RABBITMQ_DEFAULT_PASS: "password"
  RABBITMQ_ERLANG_COOKIE: "SWQOKODSQALRPCLNMEQG"   # used if clustering later
